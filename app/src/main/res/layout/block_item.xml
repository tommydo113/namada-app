<?xml version="1.0" encoding="utf-8"?>

<layout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools">

    <data>

        <variable
            name="block"
            type="com.namada.app.domain.Block" />

        <variable
            name="blockCallback"
            type="com.namada.app.ui.home.BlockClick" />
    </data>

    <com.google.android.material.card.MaterialCardView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="4dp"
        android:layout_marginBottom="4dp"
        android:onClick="@{() -> blockCallback.onClick(block)}"
        app:cardCornerRadius="0dp"
        app:cardElevation="5dp">

        <androidx.constraintlayout.widget.ConstraintLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content">

            <androidx.constraintlayout.widget.ConstraintLayout
                android:id="@+id/layout_height"
                android:layout_width="100dp"
                android:layout_height="match_parent"
                android:minHeight="60dp"
                android:background="@color/background_grey"
                app:layout_constraintTop_toTopOf="parent"
                app:layout_constraintBottom_toBottomOf="parent"
                app:layout_constraintStart_toStartOf="parent">

                <TextView
                    android:id="@+id/title"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:gravity="center"
                    android:text="Height"
                    app:layout_constraintTop_toTopOf="parent" />

                <TextView
                    android:id="@+id/block_height"
                    style="@style/TextAppearance.AppCompat.Large"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:gravity="center"
                    app:layout_constraintTop_toBottomOf="@+id/title"
                    tools:text="111111" />

                <TextView
                    android:id="@+id/tx_count"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:gravity="center"
                    android:layout_marginTop="4dp"
                    android:layout_marginBottom="4dp"
                    app:layout_constraintTop_toBottomOf="@+id/block_height"
                    tools:text="tx: 100" />
            </androidx.constraintlayout.widget.ConstraintLayout>

            <TextView
                android:id="@+id/block_hash"
                android:layout_width="0dip"
                style="@style/TextAppearance.AppCompat.Medium"
                android:layout_height="wrap_content"
                android:layout_marginStart="4dp"
                android:layout_marginTop="4dp"
                android:text="@{block.hash.toUpperCase()}"
                android:textAlignment="viewStart"
                android:textAllCaps="false"
                app:layout_constraintStart_toEndOf="@+id/layout_height"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toTopOf="parent"
                tools:text="Block hash" />

            <TextView
                android:id="@+id/proposer"
                android:layout_width="0dip"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:layout_marginBottom="4dp"
                android:textAlignment="viewStart"
                android:visibility="gone"
                style="@style/TextAppearance.AppCompat.Small"
                android:textAllCaps="false"
                app:layout_constraintStart_toEndOf="@+id/layout_height"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/block_hash" />

            <TextView
                android:id="@+id/time"
                android:layout_width="0dip"
                android:layout_height="wrap_content"
                android:layout_marginTop="4dp"
                android:layout_marginBottom="4dp"
                android:text="@{block.displayTime}"
                android:textAlignment="viewStart"
                style="@style/TextAppearance.AppCompat.Small"
                android:textAllCaps="false"
                app:layout_constraintStart_toEndOf="@+id/layout_height"
                app:layout_constraintEnd_toEndOf="parent"
                app:layout_constraintTop_toBottomOf="@+id/proposer" />

        </androidx.constraintlayout.widget.ConstraintLayout>
    </com.google.android.material.card.MaterialCardView>
</layout>